[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "skl_mindforge"
version = "3.0.0"
dependencies = ["tokenizers>=0.19.0"]

[tool.setuptools.package-data]
"skl_mindforge" = ["*.json"]